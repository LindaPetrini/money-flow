---
phase: 09-verify-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-configuration/04-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CONFIG-01, CONFIG-02, CONFIG-03, CONFIG-04, CONFIG-05, CONFIG-06, CONFIG-07]

must_haves:
  truths:
    - "VERIFICATION.md exists at .planning/phases/04-configuration/04-VERIFICATION.md"
    - "All 7 requirements are marked SATISFIED with file:line evidence citations"
    - "Build gate confirms npm run build passes with 0 errors"
    - "Test gate confirms npm test passes (all 114 tests)"
    - "REQUIREMENTS.md traceability table shows CONFIG-01 through CONFIG-07 as Complete"
  artifacts:
    - path: ".planning/phases/04-configuration/04-VERIFICATION.md"
      provides: "Formal evidence document closing all 7 Phase 4 requirements"
      contains: "status: passed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table"
      contains: "Complete"
  key_links:
    - from: ".planning/phases/04-configuration/04-VERIFICATION.md"
      to: "src/features/settings/AccountsSection.tsx"
      via: "evidence citation"
      pattern: "CONFIG-01.*SATISFIED"
    - from: ".planning/phases/04-configuration/04-VERIFICATION.md"
      to: "src/features/settings/FloorItemsSection.tsx"
      via: "evidence citation"
      pattern: "CONFIG-0[23].*SATISFIED"
    - from: ".planning/phases/04-configuration/04-VERIFICATION.md"
      to: "src/features/settings/OverflowRatiosSection.tsx"
      via: "evidence citation"
      pattern: "CONFIG-04.*SATISFIED"
    - from: ".planning/phases/04-configuration/04-VERIFICATION.md"
      to: "src/features/settings/TaxBufferSection.tsx"
      via: "evidence citation"
      pattern: "CONFIG-0[56].*SATISFIED"
    - from: ".planning/phases/04-configuration/04-VERIFICATION.md"
      to: "src/lib/bootstrap.ts"
      via: "evidence citation"
      pattern: "CONFIG-07.*SATISFIED"
---

<objective>
Formally verify that the Phase 4 (Configuration) implementation satisfies all 7 Configuration requirements. Produce VERIFICATION.md at `.planning/phases/04-configuration/04-VERIFICATION.md` with evidence for each requirement and confirm no regressions.

Purpose: Phase 4 was fully implemented but never formally verified — this is the v1.0 audit gap being closed. Formal verification separates execution claims from verified outcomes.
Output: `.planning/phases/04-configuration/04-VERIFICATION.md` (evidence document) and updated REQUIREMENTS.md traceability entries.
</objective>

<execution_context>
@/home/linda/.claude/get-shit-done/workflows/execute-plan.md
@/home/linda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-verify-configuration/09-RESEARCH.md
@.planning/phases/04-configuration/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect Phase 4 source files and gather requirement evidence</name>
  <files>src/features/settings/AccountsSection.tsx, src/features/settings/FloorItemsSection.tsx, src/features/settings/OverflowRatiosSection.tsx, src/features/settings/TaxBufferSection.tsx, src/features/settings/SettingsPage.tsx, src/lib/bootstrap.ts, src/lib/defaultConfig.ts</files>
  <action>
Read each source file in full and confirm the implementation satisfies each requirement's acceptance criteria. Record exact file:line citations for each requirement. Then run the build and test gates.

**Evidence to confirm per requirement:**

CONFIG-01 (`src/features/settings/AccountsSection.tsx`):
- `handleAdd()` creates Account with name, role, targetCents (via parseCents), and calls `setAccounts([...accounts, newAccount])`
- `handleEdit()` / `handleSaveEdit()` updates name, role, targetCents for the matching account id
- `handleDelete()` filters the account out and calls `setAccounts(filtered)` after confirm
- Five roles available: `income-hub`, `spending`, `savings`, `tax`, `investing`

CONFIG-02 (`src/features/settings/FloorItemsSection.tsx`):
- `handleAdd()` creates FloorItem with name, amountCents (parseCents), priority (parseInt), destinationAccountId, coveredThisMonth=false, expiryDate (optional), active=true
- `handleStartEdit()` / `handleSaveEdit()` updates all fields for matching item id
- `handleDelete()` removes from floorItems and calls updateSettings
- Items sorted by priority: `[...floorItems].sort((a, b) => a.priority - b.priority)`
- Destination account: select input populated from accounts

CONFIG-03 (`src/features/settings/FloorItemsSection.tsx`):
- `useEffect` runs on `floorItems` changes, checks `item.active && item.expiryDate && item.expiryDate < today`
- When expired items found: calls `updateSettings({ floorItems: updated })` with those items set to `active: false`
- Expiry date input (`type="date"`) present in add form and edit form
- Expired badge rendered in view row for visual confirmation

CONFIG-04 (`src/features/settings/OverflowRatiosSection.tsx`):
- `const total = localRatios.reduce(...)` sums all pct values
- `const isValid = Math.round(total) === 100` — Math.round avoids float false negatives
- Save button `disabled={!isValid}` with visual muted style when invalid
- "Must equal exactly 100%" warning shown when invalid
- Add/delete rows: handleAdd appends `{accountId, pctStr: '0'}`, handleDelete filters by index
- Save: `updateSettings({ overflowRatios: ratios })`

CONFIG-05 (`src/features/settings/TaxBufferSection.tsx`):
- Buffer target input (`type="number" min={0}`) for euros; initialized from `(settings.bufferTargetCents / 100).toFixed(2)`
- Buffer account select over all accounts
- Saved via `updateSettings({ ..., bufferTargetCents: parseCents(bufferTargetStr) as number as Cents, bufferAccountId })`
- Label "€ (minimum balance in income-hub account)" confirms purpose

CONFIG-06 (`src/features/settings/TaxBufferSection.tsx`):
- Tax percentage input (`type="number" min={0} max={100} step={0.01}`)
- `taxPctValid = !isNaN(taxPctNum) && taxPctNum >= 0 && taxPctNum <= 100`
- Save disabled when `!taxPctValid`; inline error "Must be 0–100" shown
- Tax account select over all accounts
- Default 37%: confirm `defaultConfig.ts` `taxPct: 37` in DEFAULT_SETTINGS

CONFIG-07 (`src/lib/bootstrap.ts` + `src/lib/defaultConfig.ts`):
- `seedIfEmpty()` called after stores load — idempotent first-run seeding
- Seeds accounts when `accounts.length === 0` with DEFAULT_ACCOUNTS (5 accounts: Tax Bucket/tax, Everyday/spending, Fun/spending, Savings/savings, Investing/investing)
- Seeds settings when `settings.overflowRatios.length === 0` with DEFAULT_SETTINGS (taxPct=37, overflowRatios 40/15/30/15 summing to 100, one floor item Rent@€1200)

**Build and test gates:**
Run from `/home/linda/projects/money-flow/`:
1. `npm run build` — must exit 0 with 0 TypeScript errors
2. `npm test` — must show all tests passing, 0 failures

Record exact output (module count from build, test count from test run) for inclusion in VERIFICATION.md.

**Note on CONFIG-07 "accept before first invoice" UX:**
The requirement says "user can accept defaults or edit before first invoice." The implementation pre-loads defaults via `seedIfEmpty()` and makes them editable via the Settings tab at any time. There is no blocking "accept/edit" modal before the first invoice. This is an implementation detail — the core requirement (defaults provided on first run) is met. Note this nuance as a caveat in VERIFICATION.md but mark CONFIG-07 as SATISFIED. Do not fix any code.
  </action>
  <verify>
    <automated>cd /home/linda/projects/money-flow && npm run build 2>&1 | tail -5 && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>All 7 source files/libs read, evidence gathered for all 7 requirements with specific file:line citations, build passes with 0 errors, test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Write 04-VERIFICATION.md and update REQUIREMENTS.md traceability</name>
  <files>.planning/phases/04-configuration/04-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <action>
Write the VERIFICATION.md file at `.planning/phases/04-configuration/04-VERIFICATION.md` and update REQUIREMENTS.md.

**CRITICAL: File path must be `.planning/phases/04-configuration/04-VERIFICATION.md` — NOT in the 09-verify-configuration directory.**

**VERIFICATION.md frontmatter:**

```yaml
---
phase: 04-configuration
verified: 2026-02-28
status: passed
requirements_checked: [CONFIG-01, CONFIG-02, CONFIG-03, CONFIG-04, CONFIG-05, CONFIG-06, CONFIG-07]
verified_by: Phase 09-verify-configuration plan 01
---
```

**Body sections:**

1. **Summary** — One paragraph: Phase 4 implementation verified. All 7 requirements satisfied. Build and test gates pass. One UX nuance (CONFIG-07 "accept before first invoice" screen) noted as caveat — does not affect core requirement satisfaction.

2. **Requirement Evidence Table** — Table with columns: Req, Status, Acceptance Criteria, Evidence, File:Line
   - CONFIG-01: SATISFIED — `handleAdd()`, `handleSaveEdit()`, `handleDelete()` for accounts; edit fields: name, role, targetCents; 5 roles available (income-hub/spending/savings/tax/investing) — cite `AccountsSection.tsx` lines for each handler
   - CONFIG-02: SATISFIED — `handleAdd()` creates FloorItem with name/amountCents/priority/destinationAccountId; sorted by priority; edit/delete handlers present — cite `FloorItemsSection.tsx` lines
   - CONFIG-03: SATISFIED — `useEffect` on `floorItems` detects `item.active && item.expiryDate && item.expiryDate < today`; calls `updateSettings({ floorItems: updated })` with `active: false`; date input present in forms — cite `FloorItemsSection.tsx` lines
   - CONFIG-04: SATISFIED — `const total = localRatios.reduce(...)`; `const isValid = Math.round(total) === 100`; Save `disabled={!isValid}`; "Must equal exactly 100%" error; add/delete row handlers — cite `OverflowRatiosSection.tsx` lines
   - CONFIG-05: SATISFIED — buffer target input (euros→parseCents→cents); buffer account select; persisted via `updateSettings({ bufferTargetCents, bufferAccountId })` — cite `TaxBufferSection.tsx` lines
   - CONFIG-06: SATISFIED — tax pct input (0-100 validated); tax account select; `taxPctValid` guard; default 37% in `DEFAULT_SETTINGS` — cite `TaxBufferSection.tsx` + `defaultConfig.ts` lines
   - CONFIG-07: SATISFIED — `seedIfEmpty()` seeds accounts when `accounts.length === 0` and settings when `overflowRatios.length === 0`; DEFAULT_ACCOUNTS has 5 accounts (tax + everyday/fun/savings/investing); DEFAULT_SETTINGS has taxPct=37 and overflowRatios summing to 100% — cite `bootstrap.ts` + `defaultConfig.ts` lines. Add caveat note.

3. **Build and Test Gates** — Section showing:
   - `npm run build`: PASSED — exact output from Task 1 (module count, 0 errors)
   - `npm test`: PASSED — N/N tests passing (record exact count from Task 1 output)
   - Date verified: 2026-02-28

4. **Caveats** — Document the CONFIG-07 UX nuance:
   - Nuance: `seedIfEmpty()` in `bootstrap.ts` pre-loads defaults on first run without showing a blocking "accept/edit" screen before the first invoice. The default configuration (tax bucket + 4 spending accounts + overflow ratios) is ready to use immediately. The user can review and edit all defaults at any time via the Settings tab.
   - Impact: The acceptance criterion "user can accept defaults or edit before first invoice" is met implicitly — defaults are pre-loaded and editable before any invoice is processed, but there is no explicit accept/edit modal.
   - Requirements affected: CONFIG-07 acceptance criterion 5 only. All other CONFIG requirements are unambiguously satisfied.

**REQUIREMENTS.md update:**

In the Requirements body section, change the 7 CONFIG checkboxes from `[ ]` to `[x]`:
```
- [ ] **CONFIG-01**: → - [x] **CONFIG-01**:
- [ ] **CONFIG-02**: → - [x] **CONFIG-02**:
- [ ] **CONFIG-03**: → - [x] **CONFIG-03**:
- [ ] **CONFIG-04**: → - [x] **CONFIG-04**:
- [ ] **CONFIG-05**: → - [x] **CONFIG-05**:
- [ ] **CONFIG-06**: → - [x] **CONFIG-06**:
- [ ] **CONFIG-07**: → - [x] **CONFIG-07**:
```

In the Traceability table, update all 7 CONFIG rows:
Change:
```
| CONFIG-01 | Phase 9 | Pending |
| CONFIG-02 | Phase 9 | Pending |
| CONFIG-03 | Phase 9 | Pending |
| CONFIG-04 | Phase 9 | Pending |
| CONFIG-05 | Phase 9 | Pending |
| CONFIG-06 | Phase 9 | Pending |
| CONFIG-07 | Phase 9 | Pending |
```
To:
```
| CONFIG-01 | Phase 4 (verified Phase 9) | Complete |
| CONFIG-02 | Phase 4 (verified Phase 9) | Complete |
| CONFIG-03 | Phase 4 (verified Phase 9) | Complete |
| CONFIG-04 | Phase 4 (verified Phase 9) | Complete |
| CONFIG-05 | Phase 4 (verified Phase 9) | Complete |
| CONFIG-06 | Phase 4 (verified Phase 9) | Complete |
| CONFIG-07 | Phase 4 (verified Phase 9) | Complete |
```

Also update the Coverage summary. Current: "Pending (gap closure): 7 (CONFIG-01/02/03/04/05/06/07 → close via Phase 9)"
After: "Pending (gap closure): 0 — all CONFIG requirements verified via Phase 9"
  </action>
  <verify>
    <automated>cd /home/linda/projects/money-flow && test -f .planning/phases/04-configuration/04-VERIFICATION.md && grep -c "SATISFIED" .planning/phases/04-configuration/04-VERIFICATION.md && grep "status: passed" .planning/phases/04-configuration/04-VERIFICATION.md && grep "Complete" .planning/REQUIREMENTS.md | grep "CONFIG-01"</automated>
  </verify>
  <done>04-VERIFICATION.md exists at correct path with status: passed and 7 SATISFIED entries. REQUIREMENTS.md shows CONFIG-01 through CONFIG-07 as Complete. REQUIREMENTS.md checkboxes for those 7 requirements changed from [ ] to [x].</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. File exists: `.planning/phases/04-configuration/04-VERIFICATION.md`
2. File contains `status: passed` in frontmatter
3. File contains 7 SATISFIED entries (one per requirement)
4. File contains build gate result (passed)
5. File contains test gate result (all tests passed)
6. File contains CONFIG-07 caveat section (UX nuance about no blocking accept modal)
7. REQUIREMENTS.md shows all 7 CONFIG requirements as Complete with [x] checkboxes
8. No source code (.ts/.tsx) files modified — `git diff src/` is empty
</verification>

<success_criteria>
- `.planning/phases/04-configuration/04-VERIFICATION.md` exists with `status: passed`
- All 7 requirements documented as SATISFIED with file:line evidence
- Build gate: `npm run build` passes (0 TS errors)
- Test gate: `npm test` passes (all tests)
- REQUIREMENTS.md updated — 7 requirements now show Complete
- Zero source code changes (verification-only phase)
</success_criteria>

<output>
After completion, create `.planning/phases/09-verify-configuration/09-01-SUMMARY.md` documenting:
- Evidence gathered for all 7 CONFIG requirements
- Build and test gate results
- CONFIG-07 caveat (no blocking accept modal — defaults pre-loaded via seedIfEmpty)
- Final state of VERIFICATION.md and REQUIREMENTS.md
- Any deviations from plan
</output>
