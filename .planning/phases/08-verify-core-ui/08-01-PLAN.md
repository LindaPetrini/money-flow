---
phase: 08-verify-core-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-core-ui/03-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [INVOICE-01, INVOICE-02, INVOICE-03, INVOICE-04, DASH-01, DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "VERIFICATION.md exists at .planning/phases/03-core-ui/03-VERIFICATION.md"
    - "All 8 requirements are marked SATISFIED with file:line evidence citations"
    - "Build gate confirms npm run build passes with 0 errors"
    - "Test gate confirms npm test passes (114 tests)"
    - "REQUIREMENTS.md traceability table shows INVOICE-01/02/03/04 and DASH-01/02/03/04 as Complete"
  artifacts:
    - path: ".planning/phases/03-core-ui/03-VERIFICATION.md"
      provides: "Formal evidence document closing all 8 Phase 3 requirements"
      contains: "status: passed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table"
      contains: "Complete"
  key_links:
    - from: ".planning/phases/03-core-ui/03-VERIFICATION.md"
      to: "src/features/invoice/InvoicePage.tsx"
      via: "evidence citation"
      pattern: "INVOICE-0[1-4].*SATISFIED"
    - from: ".planning/phases/03-core-ui/03-VERIFICATION.md"
      to: "src/features/dashboard/Dashboard.tsx"
      via: "evidence citation"
      pattern: "DASH-0[1-4].*SATISFIED"
---

<objective>
Formally verify that the Phase 3 (Core UI) implementation satisfies all 8 Core UI and Invoice requirements. Produce VERIFICATION.md at `.planning/phases/03-core-ui/03-VERIFICATION.md` with evidence for each requirement and confirm no regressions.

Purpose: Phase 3 was fully implemented but never formally verified — this is the v1.0 audit gap being closed. Formal verification separates execution claims from verified outcomes.
Output: `.planning/phases/03-core-ui/03-VERIFICATION.md` (evidence document) and updated REQUIREMENTS.md traceability entries.
</objective>

<execution_context>
@/home/linda/.claude/get-shit-done/workflows/execute-plan.md
@/home/linda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-verify-core-ui/08-RESEARCH.md
@.planning/phases/03-core-ui/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect Phase 3 source files and gather requirement evidence</name>
  <files>src/features/invoice/InvoiceForm.tsx, src/features/invoice/AllocationResult.tsx, src/features/invoice/InvoicePage.tsx, src/features/dashboard/Dashboard.tsx, src/features/dashboard/AccountCard.tsx, src/features/dashboard/ModeBadge.tsx</files>
  <action>
Read each source file in full and confirm the implementation satisfies each requirement's acceptance criteria. Record exact file:line citations for each requirement. Then run the build and test gates.

**Evidence to confirm per requirement:**

INVOICE-01 (`src/features/invoice/InvoiceForm.tsx`):
- Three controlled inputs present: amount, currency, EUR equivalent
- parseCents called on submit (or amount validated as number)
- Form submit calls parent callback with all three field values

INVOICE-02 (`src/features/invoice/AllocationResult.tsx`):
- result.moves.map() renders a card/row per move
- Each card displays: destination account name, amount (formatCents), calculation string (move.calculation), reason string (move.reason)
- All moves shown in one view (no pagination that would hide moves)

INVOICE-03 (`src/features/invoice/InvoicePage.tsx`):
- handleDone() builds updatedAccounts in a single pass
- Calls setAccounts(updatedAccounts) — all balances in one write
- Calls appendAllocation(record) — history in one append
- Both writes succeed or neither produces visible partial state (async sequential with no intermediate UI update between them)

INVOICE-04 (`src/features/invoice/InvoicePage.tsx`):
- PageState type is mutually exclusive: 'entry' | 'result' (or similar)
- handleDone() has guard: `if (state.phase !== 'result') return` (or equivalent)
- State transitions: entry → result (after allocation runs), result → entry (after Done)
- No mechanism for partially confirming some moves but not others

DASH-01 (`src/features/dashboard/Dashboard.tsx`):
- useAccountStore (or equivalent) provides accounts array
- accounts.map(account => <AccountCard ... />) renders all accounts
- No conditional filtering that would hide accounts

DASH-02 (`src/features/dashboard/AccountCard.tsx`):
- Click or double-click on balance triggers edit mode (startEditing or similar)
- Input shown during edit mode, Enter/blur commits via onBalanceChange prop
- cancelEdit or Escape cancels back to display mode

DASH-03 (`src/features/dashboard/AccountCard.tsx`):
- getStatus(account) function exists returning 'at-target' | 'near-target' | 'below-target'
- Thresholds: >=targetCents = at-target, >=targetCents*0.8 = near-target, else below-target
- STATUS_DOT_CLASSES maps each status to a bg-color class (green/yellow/red)
- Colored dot rendered in card UI using the computed class

DASH-04 (`src/features/dashboard/Dashboard.tsx` + `src/features/dashboard/ModeBadge.tsx`):
- Dashboard imports detectMode() from domain/allocationEngine or domain/modesDetection
- detectMode() called with buffer account and settings, returns 'stabilize' | 'distribute'
- mode prop passed to ModeBadge component
- ModeBadge renders mode label ('Stabilize' or 'Distribute') plus one-line explanation text

**Build and test gates:**
Run from `/home/linda/projects/money-flow/`:
1. `npm run build` — must exit 0 with 0 TypeScript errors
2. `npm test` — must show 114 passing tests, 0 failures

Record exact output (module count from build, test count from test run) for inclusion in VERIFICATION.md.

**ALLOC-02 defect scoping note:**
InvoicePage.handleDone() contains a known logic defect (ALLOC-02): floor item coverage marking uses destinationAccountId instead of floorItemId. This affects which floor items get marked covered but does NOT affect INVOICE-03 or INVOICE-04. INVOICE-03 acceptance criteria are "updates all account balances in one operation and logs allocation to history" — this works correctly. INVOICE-04 acceptance criteria are "no partial confirmation" — this also works correctly. Mark INVOICE-03 and INVOICE-04 SATISFIED. Note the ALLOC-02 defect as a caveat in VERIFICATION.md. Do not fix any code.
  </action>
  <verify>
    <automated>cd /home/linda/projects/money-flow && npm run build 2>&1 | tail -5 && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>All 6 source files read, evidence gathered for all 8 requirements with specific file:line citations, build passes with 0 errors, test suite passes with 114 tests.</done>
</task>

<task type="auto">
  <name>Task 2: Write 03-VERIFICATION.md and update REQUIREMENTS.md traceability</name>
  <files>.planning/phases/03-core-ui/03-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <action>
Write the VERIFICATION.md file at `.planning/phases/03-core-ui/03-VERIFICATION.md` and update REQUIREMENTS.md.

**CRITICAL: File path must be `.planning/phases/03-core-ui/03-VERIFICATION.md` — NOT in the 08-verify-core-ui directory.**

**VERIFICATION.md structure:**

```yaml
---
phase: 03-core-ui
verified: 2026-02-28
status: passed
requirements_checked: [INVOICE-01, INVOICE-02, INVOICE-03, INVOICE-04, DASH-01, DASH-02, DASH-03, DASH-04]
verified_by: Phase 08-verify-core-ui plan 01
---
```

Body sections:

1. **Summary** — One paragraph: Phase 3 implementation verified. All 8 requirements satisfied. Build and test gates pass. One known defect (ALLOC-02) noted as caveat — does not affect INVOICE-03/04 satisfaction.

2. **Requirement Evidence Table** — Table with columns: Req, Status, Acceptance Criteria, Evidence, File:Line
   - INVOICE-01: SATISFIED — three inputs (amount, currency, eurEquivalent) + parseCents on submit — cite InvoiceForm.tsx lines
   - INVOICE-02: SATISFIED — result.moves.map() renders card per move with accountName, formatCents(amountCents), move.calculation, move.reason — cite AllocationResult.tsx lines
   - INVOICE-03: SATISFIED — handleDone() calls setAccounts(updatedAccounts) then appendAllocation(record) sequentially; no intermediate state write between them — cite InvoicePage.tsx lines. Add caveat note referencing ALLOC-02.
   - INVOICE-04: SATISFIED — mutually exclusive PageState machine; guard at handleDone() entry; no partial-move confirmation path exists — cite InvoicePage.tsx lines
   - DASH-01: SATISFIED — accounts.map(account => AccountCard) renders all accounts without filtering — cite Dashboard.tsx lines
   - DASH-02: SATISFIED — click triggers edit mode; Enter/blur commits via onBalanceChange; Escape cancels — cite AccountCard.tsx lines
   - DASH-03: SATISFIED — getStatus() with three thresholds (>=100%, >=80%, <80% of target); STATUS_DOT_CLASSES maps to bg-green-500/bg-yellow-400/bg-red-500 — cite AccountCard.tsx lines
   - DASH-04: SATISFIED — detectMode() called in Dashboard, result passed to ModeBadge; ModeBadge renders label + explanation string for each mode — cite Dashboard.tsx and ModeBadge.tsx lines

3. **Build and Test Gates** — Section showing:
   - `npm run build`: PASSED — exact output from Task 1 (module count, 0 errors)
   - `npm test`: PASSED — 114/114 tests passing
   - Date verified: 2026-02-28

4. **Caveats** — Section documenting the ALLOC-02 known defect:
   - Defect: `InvoicePage.handleDone()` marks floor items covered using `destinationAccountId` matching, which is incorrect when multiple floor items share the same destination account. The correct field is `floorItemId`.
   - Impact: In edge cases (multiple floor items → same account), only the first floor item gets marked covered after confirmation. Account balance update and history append are unaffected.
   - Requirements affected: ALLOC-02 (floor coverage accuracy). INVOICE-03 and INVOICE-04 are NOT affected — their acceptance criteria concern atomicity of balance updates and history, not floor item matching accuracy.
   - Fix planned: Phase 10 (Fix Integration Defects).

**REQUIREMENTS.md update:**
In the Traceability table, update all 8 requirement rows to show Status = Complete:

Change these rows (they currently show "Pending"):
```
| INVOICE-01 | Phase 8 | Pending |
| INVOICE-02 | Phase 8 | Pending |
| INVOICE-03 | Phase 8 | Pending |
| INVOICE-04 | Phase 8 | Pending |
| DASH-01 | Phase 8 | Pending |
| DASH-02 | Phase 8 | Pending |
| DASH-03 | Phase 8 | Pending |
| DASH-04 | Phase 8 | Pending |
```

To:
```
| INVOICE-01 | Phase 3 (verified Phase 8) | Complete |
| INVOICE-02 | Phase 3 (verified Phase 8) | Complete |
| INVOICE-03 | Phase 3 (verified Phase 8) | Complete |
| INVOICE-04 | Phase 3 (verified Phase 8) | Complete |
| DASH-01 | Phase 3 (verified Phase 8) | Complete |
| DASH-02 | Phase 3 (verified Phase 8) | Complete |
| DASH-03 | Phase 3 (verified Phase 8) | Complete |
| DASH-04 | Phase 3 (verified Phase 8) | Complete |
```

Also update the checkboxes in the Requirements section body (lines starting with `- [ ] **INVOICE-` and `- [ ] **DASH-`) to `- [x]`.

Also update the Coverage summary line that reads "Pending (gap closure): 15" to "Pending (gap closure): 7" (8 requirements just closed, CONFIG-01 through CONFIG-07 remain pending).
  </action>
  <verify>
    <automated>cd /home/linda/projects/money-flow && test -f .planning/phases/03-core-ui/03-VERIFICATION.md && grep -c "SATISFIED" .planning/phases/03-core-ui/03-VERIFICATION.md && grep "status: passed" .planning/phases/03-core-ui/03-VERIFICATION.md && grep "Complete" .planning/REQUIREMENTS.md | grep "INVOICE-01"</automated>
  </verify>
  <done>03-VERIFICATION.md exists at correct path with status: passed and 8 SATISFIED entries. REQUIREMENTS.md shows INVOICE-01/02/03/04 and DASH-01/02/03/04 as Complete. REQUIREMENTS.md checkboxes for those 8 requirements changed from [ ] to [x].</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. File exists: `.planning/phases/03-core-ui/03-VERIFICATION.md`
2. File contains `status: passed` in frontmatter
3. File contains 8 SATISFIED entries (one per requirement)
4. File contains build gate result (passed)
5. File contains test gate result (114 tests passed)
6. File contains ALLOC-02 caveat section
7. REQUIREMENTS.md shows all 8 Phase 3 requirements as Complete with [x] checkboxes
8. No source code (.ts/.tsx) files modified — `git diff src/` is empty
</verification>

<success_criteria>
- `.planning/phases/03-core-ui/03-VERIFICATION.md` exists with `status: passed`
- All 8 requirements documented as SATISFIED with file:line evidence
- Build gate: `npm run build` passes (0 TS errors)
- Test gate: `npm test` passes (114 tests)
- REQUIREMENTS.md updated — 8 requirements now show Complete
- Zero source code changes (verification-only phase)
</success_criteria>

<output>
After completion, create `.planning/phases/08-verify-core-ui/08-01-SUMMARY.md`
</output>
